import { Badge, Card, ProgressBar } from "react-bootstrap";
export default function RoleMatchCard({ role }) { return <Card className="border-0 shadow-sm mb-3"><Card.Body><div className="d-flex justify-content-between"><Card.Title className="h5">{role.name}</Card.Title><strong className="text-primary">{role.match}% match</strong></div><ProgressBar now={role.match} className="mb-3" /><p className="small mb-2">{role.explanation}</p><div className="small mb-1"><strong>Matching:</strong> {role.matching.map((x) => <Badge className="me-1" bg="success" key={x}>{x}</Badge>)}</div><div className="small mb-2"><strong>Missing:</strong> {role.missing.map((x) => <Badge className="me-1" bg="warning" text="dark" key={x}>{x}</Badge>)}</div><small className="text-muted">{role.alignment}</small></Card.Body></Card>; }
