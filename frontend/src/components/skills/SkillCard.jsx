import { Badge, Card, ProgressBar } from "react-bootstrap";
export default function SkillCard({ skill }) { const tone = skill.source === "Explicit" ? "success" : skill.source === "Inferred" ? "info" : "warning"; return <Card className="h-100 shadow-sm border-0"><Card.Body><div className="d-flex justify-content-between gap-2"><div><Card.Title className="h6">{skill.name}</Card.Title><small className="text-muted">{skill.category}</small></div><Badge bg={tone}>{skill.source}</Badge></div><div className="mt-3 small d-flex justify-content-between"><span>Proficiency</span><strong>{skill.proficiency}%</strong></div><ProgressBar now={skill.proficiency} className="mb-2" /><div className="small">Confidence: <strong>{skill.confidence}%</strong></div><div className="small text-muted mt-2">Evidence: {skill.evidence}</div><div className="small text-muted">Updated: {skill.updated}</div></Card.Body></Card>; }
