import { Button, Card, Badge } from "react-bootstrap";
export default function CourseCard({ course }) { return <Card className="h-100 shadow-sm border-0"><Card.Body className="d-flex flex-column"><Card.Title className="h6">{course.title}</Card.Title><div className="small text-muted mb-2">{course.provider} · {course.duration}</div><div className="mb-2"><Badge bg="info">{course.skill}</Badge> <Badge bg="light" text="dark">{course.difficulty}</Badge></div><p className="small flex-grow-1">{course.reason}</p><Button size="sm" variant="outline-primary" href={course.url} target="_blank" rel="noreferrer">View course</Button></Card.Body></Card>; }
