# Integration adapters

Adapters expose optional external sources behind stable client/parser/adapter boundaries. Local JSON catalogs remain the development fallback; no connector is active in this scaffold.
