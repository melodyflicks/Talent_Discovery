# Integrations

GitHub, LMS, competitive data, and course catalogs are adapter boundaries. Integrations are optional; local data is always the fallback. Course recommendation must rank returned catalog metadata rather than allowing an LLM to invent courses.
