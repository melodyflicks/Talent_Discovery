# API

`GET /health` confirms the service is available. Route shells are grouped under `/api/v1` for auth, employees, profiles, skills, roles, matching, skill-gaps, courses, roadmap, assistant, and HR. Interactive contracts are available from `/docs`.
