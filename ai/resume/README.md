# Resume pipeline

Future processing flow: PDF/DOCX → preprocessing → existing/customized extraction model → extracted fields → configured LLM → structured employee profile. No new resume model is trained here.
